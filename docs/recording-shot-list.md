# Three-minute recording shot list

This is a recording plan, not a recorded submission. Every command and claim below is backed by merged local behavior. Do not substitute fixtures, browser text, or edits for unavailable external evidence.

## 0:00–0:20 — fixed-price agent work

**Screen:** `README.md` architecture diagram and the local browser landing page.

**Voiceover / caption:** “Agentopoly lets a buyer hire an independent provider on fixed signed terms, verify a concrete artifact, and retain local evidence. The browser is a projection; it cannot authorize payment.”

## 0:20–0:45 — P2P capability discovery

**Screen:** terminal command and its passing output.

```nu
bun run test:integration:pear
```

**Voiceover / caption:** “This runs two real local Pear hosts and admits a signed capability envelope into the local market. It is a local discovery proof, not a centralized registry.”

## 0:45–1:15 — real provider and fixed terms

**Screen:** run the bounded provider command and show only the returned workspace identifier.

```nu
bun run agentopoly provider reliable-provider --print "Implement the reviewed fixture exactly."
```

**Voiceover / caption:** “The provider works in a confined repository fixture. Before verification, the operator supplies a workspace-bound signed agreement through the reviewed local workflow. Never show signing material.”

## 1:15–1:50 — objective verification and guarded settlement

**Screen:** run the second rehearsal phase for the returned workspace.

```nu
nu scripts/rehearse-recorded-demo.nu settle .tmp/agentopoly-runs/<workspace>
```

**Voiceover / caption:** “The fixed verifier hashes the artifact and records bounded evidence. Finalize checks the strict agreement witness and local policy twice. The second call must append no new decision evidence.”

**Required label:** “WDK broadcast unavailable: this build records a typed refusal unless every future broadcast witness and enabled adapter are present.”

## 1:50–2:20 — local evidence and browser projection

**Screen:** start the projection and open its local URL.

```nu
bun run dev
```

**Voiceover / caption:** “The browser shows bounded local provider, verification, refusal, and reputation evidence. Reputation is local evidence, not a global score.”

## 2:20–2:45 — safety boundary

**Screen:** `docs/submission-evidence.md` and the linked policy/reservation source files.

**Voiceover / caption:** “Peers, models, and browsers cannot select a wallet operation. Payment requires exact source, destination, token, amount, fee, agreement, artifact, verification, preview, and durable reservation witnesses.”

## 2:45–3:00 — honest close

**Screen:** this checklist and the submission evidence manifest.

**Voiceover / caption:** “Funded WDK broadcast, transaction observation, paid arbitration, escrow, and a recorded submission are not available evidence yet. This slot is reserved for later uncut proof.”

- Explorer link: `<replace after funded broadcast proof>`
- Uncut sponsor clip: `<replace after recording>`
- Recorded-video link: `<replace after recording>`

## Operator checklist

- Use [the rehearsal runbook](./recording-rehearsal.md) and [submission evidence manifest](./submission-evidence.md).
- Do not load a wallet, WDK daemon, seed, passphrase, or private signing material for this plan.
- Do not claim arbitration, funded settlement, or receipt finality from a typed refusal.
