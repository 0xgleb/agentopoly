// Typechecking shim for the repository fixture only. The CLI does not copy this file;
// each live provider must create its own submission.ts in a disposable workspace.
export { normalizeMarketHandle } from './starter.ts'
